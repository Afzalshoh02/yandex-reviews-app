services:
  - type: web
    name: yandex-reviews-app
    env: docker
    docker:
      dockerfile: Dockerfile
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: APP_KEY
        generateValue: true
    healthCheckPath: /health
    autoDeploy: true
